Run (TUI): `python main.py`
Run (CLI): `python main.py --cli tree.txt -t ./output [--dry-run]`
No lint/format/test commands defined in repo; standard Python tooling likely (if needed: `python -m pytest`, `python -m ruff`, `python -m black`).
Utilities: use `git status`, `git diff` as needed.